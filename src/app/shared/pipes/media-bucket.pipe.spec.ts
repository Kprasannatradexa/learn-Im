import { MediaBucketPipe } from './media-bucket.pipe';

describe('MediaBucketPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaBucketPipe();
    expect(pipe).toBeTruthy();
  });
});
